#!/usr/bin/env python3

# Incorrectly written assert (note: nice that the default settings of running Python detect this incorrectly written assert (with the tuple) and triggers an ERROR).
#assert( True, "no problem" )

#assert( False, "trigger the assert" )

# Correctly written assert
assert True, "no problem"

assert False, "trigger the assert"

